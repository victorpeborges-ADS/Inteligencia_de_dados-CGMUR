# Manchas oficiais de validação (20h.5)

Deposite GeoJSON por município:

```text
backend/data/manchas_oficiais/2611606.geojson
```

A camada `manchas_oficiais` aparece no mapa (selo Oficial). Na simulação de chuva, o meta inclui `validacao_mancha_oficial` (IoU / coberturas) quando o arquivo existe.
